interface:
  display_name: "Review Vitest Snapshots"
  short_description: "Safely run, review, and apply Vitest snapshots"
  default_prompt: "Use $review-vitest-snapshots to run the tests, inspect candidate snapshot changes, and safely apply approved updates."
