interface:
  display_name: "Review Vitest Snapshots"
  short_description: "Compact repeated snapshot changes safely"
  default_prompt: "Use $review-vitest-snapshots to compact repeated Vitest failures into exact families, review each distinct change once, and safely apply approved updates."
